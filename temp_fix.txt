    }, [searchTerm, statusFilter]); // eslint-disable-line react-hooks/exhaustive-deps

    const handleExpiryAction = useCallback(async (userId, action, hours) => {
      await handleUpdateExpiry(userId, action, hours, '', loadExpiryUsersLocal);
    }, [handleUpdateExpiry, loadExpiryUsersLocal]);

    const getStatusBadge = (user) => {